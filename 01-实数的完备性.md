# 讲义 01 · 实数的完备性

> **本讲目标**：说清楚实数 ℝ 到底比有理数 ℚ "多"了什么。答案是一个词：**完备性**。
> 我们将把它凝练成一条能反复使用的工具——**确界原理**。这是整座大厦**唯一的公理性地基**，
> 后面所有"存在性"定理最终都会踩在它上面。

**主定理预告**：⭐ **确界原理**（有上界的非空实数集必有上确界）。

---

## 1.1 动机：有理数哪里不够用了？

你可能觉得有理数已经很够用了——它们在数轴上**稠密**，任意两个有理数之间还夹着无穷多个有理数，密密麻麻，看起来根本没有空隙。

但有理数有一个致命缺陷：**它有"洞"。**

### 一个经典的洞：$\sqrt{2}$

考虑这个有理数集合：

$$
A = \{\, x \in \mathbb{Q} : x > 0 \text{ 且 } x^2 < 2 \,\}.
$$

也就是"平方小于 2 的正有理数"。这个集合显然有上界（比如 2 就是一个上界，因为 $2^2=4>2$）。

现在问一个非常自然的问题：**$A$ 有没有一个"最小的上界"？**

- $1.5$ 是上界吗？$1.5^2 = 2.25 > 2$，是。
- $1.42$ 是上界吗？$1.42^2 = 2.0164 > 2$，是，而且更紧。
- $1.415$？$1.415^2 = 2.002\ldots > 2$，还是。
- ……

你会发现，你可以让上界越来越接近某个数，但这个"临界数"是谁？
它应该满足 $x^2 = 2$，也就是 $x=\sqrt 2$。可是——

> **📎 命题**：$\sqrt 2$ 不是有理数。

**证明**（反证法，这是最经典的存在性反证）。假设 $\sqrt2 = p/q$，其中 $p,q$ 是整数且已约到**互质**（没有公因子）。两边平方：$p^2 = 2q^2$。于是 $p^2$ 是偶数，从而 $p$ 是偶数（奇数的平方是奇数）。设 $p=2k$，代入得 $4k^2 = 2q^2$，即 $q^2 = 2k^2$，于是 $q$ 也是偶数。但这样 $p,q$ 都能被 2 整除，与"互质"矛盾。$\blacksquare$

**这意味着什么？** 在有理数的世界里，集合 $A$ **有上界，却没有最小上界**。
那个本该做"最小上界"的数 $\sqrt2$，在数轴上有理数的位置上**是一个洞**——你无限逼近它，却永远抓不到它。

这就是有理数的缺陷。而这个缺陷会**直接摧毁微积分**：

- 如果连"$x^2=2$ 有解"都不能保证，那"介值定理"（连续函数取遍中间值）就是假的；
- 如果一个明明在往某处收敛的序列，其极限却"不在场"，那整个极限理论就无从谈起。

**实数就是为了填掉所有这些洞而造出来的。** 怎么填？下面就来精确地说。

---

## 1.2 上界、上确界：把"最小上界"这件事说清楚

在谈"完备"之前，我们得先有精确的语言。

### 定义 1.1（上界 / 下界）
设 $S \subseteq \mathbb{R}$ 是非空实数集。
- 称 $M$ 是 $S$ 的一个**上界**，如果 $\forall x \in S,\ x \le M$。
- 称 $m$ 是 $S$ 的一个**下界**，如果 $\forall x \in S,\ x \ge m$。
- 若 $S$ 有上界，称 $S$ **有上界**（bounded above）；有上界又有下界，称 $S$ **有界**。

注意：上界**不唯一**。如果 $M$ 是上界，那么 $M+1, M+100$ 也都是上界。
所以问"上界是谁"没意义，有意义的问题是——**最小的那个上界是谁**。

### 定义 1.2（上确界 supremum）⭐
称实数 $\alpha$ 是 $S$ 的**上确界**，记作 $\alpha = \sup S$，如果它同时满足两条：

1. **（是上界）** $\forall x \in S,\ x \le \alpha$；
2. **（是最小的上界）** 对任何 $\varepsilon > 0$，都存在某个 $x \in S$ 使得 $x > \alpha - \varepsilon$。

第 2 条值得多看一眼：它说的是"你只要比 $\alpha$ 小一丁点（小 $\varepsilon$），就**不再是上界了**——
因为集合里能找到超过 $\alpha-\varepsilon$ 的元素"。这正是"**最小**上界"的精确含义。

> **为什么用 $\varepsilon$ 来表达"最小"？** 因为"最小上界"= "所有上界里最小的那个"，
> 直接说会绕。换个等价说法：任何**严格小于** $\alpha$ 的数都不是上界。
> 而"严格小于 $\alpha$"可以写成 $\alpha - \varepsilon$（$\varepsilon>0$）。这就是第 2 条的来历。
> 你会在讲义 02 看到，这种"用 $\varepsilon$ 把某种极限性质翻译成可检验命题"的手法，是整门课的通用语言。

**下确界** $\inf S$ 完全对称地定义：最大的下界。

⚠️ **易错点**：$\sup S$ **不一定属于 $S$**。
例如开区间 $S=(0,1)$，$\sup S = 1$，但 $1 \notin S$。
如果 $\sup S$ 恰好属于 $S$，我们就叫它**最大值** $\max S$。
"上确界存在"比"最大值存在"要弱、要容易满足——这个区别在讲义 04 会变得极其重要。

---

## 1.3 地基公理：确界原理 ⭐

现在到了整门课最关键的一句话。有理数的问题是"有上界却没有上确界"（$A$ 的上确界 $\sqrt2$ 是个洞）。
实数被构造出来，就是为了**堵死这种情况**。我们把这件事当作**公理**接受下来：

### 公理（确界原理 / 完备性公理，Least Upper Bound Property）⭐
> **实数集 ℝ 的任何非空、有上界的子集，都有上确界（$\sup$ 存在，且是一个实数）。**

对称地也有：任何非空、有下界的实数集都有下确界。

**这就是"完备性"的精确含义，也是 ℝ 和 ℚ 的唯一本质区别。**
ℚ 不满足这条（$A$ 是反例）；ℝ 满足。你可以把 ℝ 想成"把 ℚ 的所有洞都填上之后得到的东西"。

> **为什么把它当公理，而不是证明它？**
> 因为它取决于你**怎么构造实数**。如果用戴德金分割（Dedekind cut）或柯西序列来
> 严格建造 ℝ，那么确界原理可以被**证明**。但那套构造属于更深的实分析，
> 本课把它当作**出发点**接受——这完全合法，就像几何学从公理出发一样。
> **你需要记住的是：这是我们唯一"白拿"的东西，之后一切都要靠它挣。**

---

## 1.4 完备性的三副面孔：等价表述

确界原理有几个"长得不一样、其实是同一件事"的兄弟。它们**互相等价**，
在不同证明里用哪个方便就用哪个。你不用现在就背下所有等价性证明，
但要知道它们是**同一个完备性**的不同外衣。

### 面孔一：单调有界收敛定理（MCT）📎
> **单调递增且有上界的实数序列必收敛**（收敛到它的上确界）。

**为什么它和确界原理是一回事？** 直觉上：一个往上爬、又有天花板的序列，
只能越来越挤向天花板下方某处，那个"某处"正是上确界。我们把这个直觉做严格。

**证明（确界原理 ⟹ MCT）。**
设 $(x_n)$ 单调递增且有上界。令 $S = \{x_1, x_2, \dots\}$，这是非空有上界的集合，
由确界原理 $\alpha = \sup S$ 存在。我断言 $x_n \to \alpha$。

任取 $\varepsilon > 0$。由上确界定义的第 2 条，存在某个 $x_N > \alpha - \varepsilon$。
又因为序列单调递增，对所有 $n \ge N$ 有 $x_n \ge x_N > \alpha - \varepsilon$。
同时 $\alpha$ 是上界，所以 $x_n \le \alpha < \alpha + \varepsilon$。合起来：
$$
n \ge N \implies \alpha - \varepsilon < x_n \le \alpha \implies |x_n - \alpha| < \varepsilon.
$$
这正是 $x_n \to \alpha$ 的定义（ε-N 语言，见讲义 02）。$\blacksquare$

> 这个证明是**全课"确界 → 收敛"的样板**。请记住它的套路：
> **构造集合 → 取上确界 → 用上确界的"逼近"性质（第 2 条）抓出下标 $N$**。
> 你会在讲义 03、04、08 一次次看到它的变体。

### 面孔二：区间套定理（Nested Interval Theorem）📎
> 若闭区间序列 $[a_1,b_1] \supseteq [a_2,b_2] \supseteq \cdots$ 层层嵌套，
> 且长度 $b_n - a_n \to 0$，则存在**唯一**的实数 $\xi$ 属于所有这些区间。

**为什么需要它？** 这是"用夹逼锁定一个点"的工具。很多存在性证明的策略是：
不断把区间**对半砍**，让目标被越夹越紧，最后区间套定理保证被夹的那个点**真的存在**
（在 ℚ 里它可能不存在——又是洞的问题）。讲义 03 的 Bolzano–Weierstrass 就靠它。

**证明思路（用 MCT）。** 左端点 $(a_n)$ 单调递增有上界（被任一 $b_m$ 界住），
故收敛到某 $\xi$；右端点 $(b_n)$ 单调递减有下界，收敛到某 $\eta$。
由 $b_n-a_n\to 0$ 得 $\xi=\eta$，且它落在每个区间里。唯一性由长度趋于 0 保证。$\blacksquare$

### 面孔三：阿基米德性质（Archimedean Property）📎
> 对任何实数 $x$，都存在自然数 $n$ 使得 $n > x$。（"没有无穷大的实数"）

等价地：对任何 $\varepsilon>0$，存在自然数 $n$ 使 $\tfrac1n < \varepsilon$。（"没有正的无穷小"）

**为什么它重要？** 请回到讲义 00 那个"无穷小的鬼魂"。
阿基米德性质**从制度上禁止了无穷小的存在**：任何正数，不管多小，都有个 $1/n$ 比它还小，
但没有哪个正数小于**所有** $1/n$。这一条，就是我们赶走无穷小的法律依据。

**证明（确界原理 ⟹ 阿基米德性质）。** 反证：若存在实数 $x$ 使得所有自然数 $n \le x$，
则 $\mathbb{N}$ 有上界，由确界原理有上确界 $\alpha=\sup\mathbb N$。
那么 $\alpha-1$ 不是上界，故存在自然数 $m > \alpha-1$，于是 $m+1 > \alpha$——
但 $m+1$ 也是自然数，超过了上确界 $\alpha$，矛盾。$\blacksquare$

---

## 1.5 小结：地基已成

这一讲我们只干了一件事，但它是整座大厦最重要的一件事：

- 有理数有**洞**（$\sqrt2$ 处上确界缺失），不足以支撑微积分。
- 实数的本质特征是**完备性**，精确表述为⭐**确界原理**：非空有上界的实数集必有上确界。
- 完备性有几副等价面孔：**单调有界收敛（MCT）**、**区间套**、**阿基米德性质**，
  以后哪个顺手用哪个。
- 我们建立了一个将反复出现的**证明套路**："构造集合 → 取确界 → 用确界的逼近性质抓下标"。

**记住这句话，它是本讲的灵魂**：

> 从现在起，每当某个定理断言"某个东西**存在**"（极限、最值、零点、原函数……），
> 你都可以顺着证明往下挖，最终一定会挖到**确界原理**这块地基。挖不到，说明证明有漏洞。

下一讲，我们用完备性做第一件大事：把"极限"从"无穷小的鬼魂"手里救出来，
交给严谨的 **ε 语言**。

→ [讲义 02：极限与 ε 语言](02-极限与ε语言.md)

---

### 思考题

1. 写出 $\sup$ 定义的第 2 条对集合 $S=(0,1)$ 的具体检验：为什么 $0.999$ 不是上确界，而 $1$ 是？
2. 集合 $S=\{\,1-\tfrac1n : n\in\mathbb N\,\}=\{0,\tfrac12,\tfrac23,\dots\}$ 的上确界是多少？它属于 $S$ 吗？有最大值吗？
3. 用阿基米德性质证明：不存在一个正实数 $\delta$ 小于所有的 $\tfrac1n$。（这就是"无穷小不存在"的严格版本。）
4. 回到讲义 00 思考题 3：如果把整门课建在 ℚ 而非 ℝ 上，介值定理为什么会崩？给一个具体的反例函数。
